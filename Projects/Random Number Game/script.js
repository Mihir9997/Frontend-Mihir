'use strict;'
const numEL = document.querySelector('.num')
const data = document.querySelectorAll

function numEve(result) {
    console.log(numEL.textContent);

}

// for(let i=1, i)

numEL.addEventListener('click', numEve)
